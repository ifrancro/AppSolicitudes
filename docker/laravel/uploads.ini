; Limites de subida para los adjuntos (contrato 5.4: 5 MB por archivo).
; Deben superar el maximo validado por Laravel para que este responda con 422 y no PHP con un fallo silencioso.
upload_max_filesize=6M
post_max_size=12M
